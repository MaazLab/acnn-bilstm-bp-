# ACNN-BiLSTM — Pending Changes
**Paper**: Cui et al., *Bioengineering* 2024, 11, 306.

---

## Baseline Results
| Run | SBP MAE | DBP MAE | BHS Grade |
|-----|---------|---------|-----------|
| Baseline — no label normalization | ~24 mmHg (plateau) | ~24 mmHg (plateau) | — |
| After Issue #1 (BPScaler) | 19.33 mmHg | 10.84 mmHg | D |
| After Issue #3 (SELU) | TBD | TBD | TBD |
| After Issue #4 (CWT scales) | TBD | TBD | TBD |
| Paper target (Cui et al.) | 1.67 ± 5.28 mmHg | 1.15 ± 2.53 mmHg | A |

---

## Changes

| # | Issue | Comments | Implemented? |
|---|-------|----------|:---:|
| 1 | **Missing target (BP label) normalization** | **Root cause**: Raw BP labels (SBP ~90–200, DBP ~50–120 mmHg) were fed directly as regression targets. L1Loss settled at predicting the dataset mean (~127/73 mmHg), which is a trivial minimum. Model output range was compressed to 47–67% of the true range. **Evidence**: Overfit test without normalization → best MAE = 9.39 mmHg `[FAIL]`; with z-score normalization → best MAE = 3.57 mmHg, R² > 0.9 `[PASS]`. Mean-baseline MAE = 13.38 mmHg; plateau at 24 mmHg was *worse* than the mean predictor. **Fix**: Added `BPScaler` (z-score, fitted on training windows only) in `acnn_bilstm/data/scaler.py`. | ✅ |
| 2 | **Strided conv instead of MaxPool for spatial downsampling** | **Root cause**: Used `Conv2d(stride=2)` inside ACNN blocks to halve spatial dimensions. Paper Figure 3 shows Conv2D and pooling as separate stages; strided conv discards intermediate positions, while MaxPool2d(2,2) selects the strongest activation per 2×2 region. **Evidence**: Paper Figure 3 diagram separates Conv2D and pooling clearly. **Fix**: Changed `Conv2d(stride=2)` → `Conv2d(stride=1)` + `MaxPool2d(2,2)` in `acnn_bilstm/model/acnn_block.py` (commit d4c2086). Pool pattern `[T,F,T,F,T,T,T]` preserves same spatial reduction 128×256 → 4×8. | ✅ |
| 3 | **Wrong activation function — ReLU instead of SELU** | **Root cause**: Figure 3 labels the activation "ReLU", but Section 3.3 states: *"the self-activation function…self-normalization ability"*. Self-normalization is a property unique to SELU (Klambauer et al., NeurIPS 2017, arXiv:1706.03462). ReLU has a dying-neuron problem and lacks self-normalization, degrading gradient flow across 7 stacked ACNN blocks. SELU also requires `inplace=False` — inplace SELU corrupts autograd for negative-region values. **Evidence**: No activation other than SELU provides a theoretical self-normalization guarantee. Paper text takes precedence over a simplified figure label. **Fix**: `nn.ReLU(inplace=True)` → `nn.SELU()` in `acnn_block.py` and `acnn_bilstm.py`; `F.relu` → `F.selu` in FC forward pass. | ✅ |
| 3b | **Wrong operation order — SELU before BN instead of BN before SELU** | **Root cause**: Figure 3 label implies Conv→Act→BN, but paper text (Section 3.3) states *"BN alleviates gradient dispersion… **Then**, the self-activation function"* — BN before SELU. Applying SELU before BN is logically self-defeating: BN's learned γ/β parameters immediately undo SELU's normalisation, destroying the self-normalisation guarantee. Correct order (Conv → BN → SELU) follows the Pre-Activation pattern (He et al., ResNet v2, 2016): BN brings raw conv output to ~N(0,1), which is exactly the input condition SELU's fixed-point guarantee requires. **Fix**: Changed forward in `acnn_block.py` from `conv→act→bn` to `conv→bn→act`; same fix in `_forward_cnn` of `acnn_bilstm.py`. | ✅ |
| 4 | **CWT scales deviate from paper — log (25–400) instead of linear (1–128)** | **Root cause**: Paper Section 2.2 states "The number of scales is set to 128", implying the default PyWavelets linear range `np.arange(1, 129)`. Our config uses `np.logspace(log10(25), log10(400), 128)`, which covers a different frequency range entirely. For `cgau1` at 200 Hz: linear 1–128 gives fine-grained high-frequency resolution (cardiac harmonics, dicrotic notch); our log 25–400 misses scales < 25. This produces fundamentally different CWT images — the model trains on different visual features than the paper. **Evidence**: `config.py` → `cwt_scale_type = "log"`; paper default is linear. This is likely the dominant cause of the 19.33 vs 1.67 mmHg gap. **Fix**: Change `cwt_scale_type` default to `"linear"` in `config.py`; verify `cwt_transform.py` uses `np.arange(1, n_scales + 1)`; delete cached CWT files to force regeneration. | ☐ |
| 5 | **Overfitting — large model, small effective dataset** | **Root cause**: 4.88M parameters, but only ~130 independent training subjects (effective samples = subjects, not windows, because all windows per subject share the same BP label). Model memorises training subjects but fails on unseen test subjects. **Evidence**: Train MAE ~4 mmHg vs Test SBP MAE 19.33 mmHg. Systematic bias SBP ME = −9.20 mmHg (test subjects have higher BP than training mean). Early stopping at epoch 25. **Fix candidates**: Increase dropout 0.2 → 0.5 in FC layers (paper does not specify rate); add L2 weight decay to Adam. Monitor after Issue #4 is resolved — correct CWT features may generalise better. | ☐ |
| 6 | **10-fold cross-validation not used for final reporting** | **Root cause**: Paper uses 10-fold CV on the 80% training split and reports mean ± std across folds (Table 2). Our pipeline runs CV only when `--cv` flag is set; default is a single train/test split. Without CV, our single-run numbers are not directly comparable to the paper's reported values. **Fix**: Enable `--cv` flag for final evaluation runs after Issues #3–5 are resolved. | ☐ |
